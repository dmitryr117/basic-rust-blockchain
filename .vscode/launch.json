{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Tests",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/deps/cryptochain-{hash}",
      "args": ["--nocapture"],
      "cwd": "${workspaceFolder}"
    }
  ]
}